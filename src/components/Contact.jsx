import React from 'react'

const Contact = () => {

    return (
      <div>
        <main style={mainStyle}>
        </main>
      </div>
    );
}

export default Contact

const mainStyle={
    background: 'yellow',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '90vh',
}